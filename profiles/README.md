# Install profiles (metapackages)

| Profile | Role |
|---------|------|
| `core` | Always-on Nabla Net / Edge basics |
| `inference` | NVIDIA GPU share stack (`nabla-inference`, Desktop only) |
| `kiosk` | Optional HA / kiosk helpers |

Stubs only until packages exist.
